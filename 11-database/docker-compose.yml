version: '3.8'

services:
    app:
        build: '.'
        networks:
            - local
        ports:
            - 8000:8000
        volumes:
            - ./app:/usr/src/app
    # sqlite:
    #     image: keinos/sqlite3:latest

networks:
  local:
    name: local
